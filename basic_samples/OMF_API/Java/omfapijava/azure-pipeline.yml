variables:
  projpath: /basic_samples/OMF_API/Java/omfapijava
  CredFile: config.properties
  CredFileLibrary: config.properties
  testpath: /basic_samples/OMF_API/Java/omfapijava

jobs:      
- job: OMF_APIJava

  pool:
    vmImage: 'Ubuntu-16.04'
  steps :

    # Download Secure File
    # Download a secure file to a temporary location on the build or release agent
    - task: DownloadSecureFile@1
      inputs:
        secureFile: $(CredFileLibrary)

    # Copy Files
    # Copy files from source folder to target folder using match patterns (The match patterns will only match file paths, not folder paths)
    - task: CopyFiles@2
      inputs:
        sourceFolder: $(Agent.TempDirectory)
        contents: $(CredFileLibrary)
        targetFolder: '$(Build.SourcesDirectory)$(projpath)/'
        #cleanTargetFolder: false # Optional
        overWrite: true # Optional
        #flattenFolders: false # Optional
      displayName: copy file
      
    - task: Maven@3
      inputs:
        mavenPomFile: '$(Build.SourcesDirectory)$(projpath)/pom.xml'
        publishJUnitResults: false
        testResultsFiles: '**/TEST-*.xml'
        goals: 'test'
 